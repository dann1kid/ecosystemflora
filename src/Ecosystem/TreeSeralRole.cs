namespace WildFarming.Ecosystem
{
    /// <summary>Forest succession role for wild tree spread (pioneer → mid → climax).</summary>
    internal enum TreeSeralRole
    {
        Pioneer,
        Mid,
        Climax,
    }
}
